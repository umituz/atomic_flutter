name: atomic_flutter_kit_example
description: Demonstrates how to use the atomic_flutter_kit package.
publish_to: 'none' # Remove this line if you wish to publish this package.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  atomic_flutter_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
